# A28 — Live n8n publish (2026-05-21)

Published repo workflow exports to production n8n via MCP SDK (`validate_workflow` → `update_workflow` → `publish_workflow`).

## Workflows

| Workflow | ID | URL | Active version |
|----------|-----|-----|----------------|
| `cdp_analise` | `6id6dkinK9xTLfsb` | https://automacao.tktechnologies.com.br/workflow/6id6dkinK9xTLfsb | `c6b365c5-e13c-4c62-9057-a67ddb92f3ee` |
| `cdp_resultado` | `VfBSV3WU6on8BXm8` | https://automacao.tktechnologies.com.br/workflow/VfBSV3WU6on8BXm8 | `78988226-6efc-45cc-95ee-e24bf54b4350` |

## Contract fixes (was drift)

**cdp_analise**
- POST `/api/v1/jobs` body: `items`, `sites`, `callback_url`, `priority` only (no `job_metadata`)
- Default sites: `gm`, `ml`, `vw`, `eu`, `pecadireta` (no `ebay`)
- Batch formatter v1.0 with callback query-param context

**cdp_resultado**
- Webhook secret IF: `$env.CDP_CALLBACK_WEBHOOK_SECRET || ...` (was hardcoded `changeme`)

## Tooling added

- `scripts/n8n_workflow_json_to_sdk.mjs` — converts repo `n8n/workflows/*.json` to SDK for MCP publish

## Post-publish checks

1. Confirm Gmail, Telegram, and Google Sheets credentials still attached in n8n UI (SDK publish may skip credential auto-assign).
2. Run dispatcher from Telegram/Sheets smoke path when convenient.
3. Callback path already validated with A27 E2E (execution 369).
