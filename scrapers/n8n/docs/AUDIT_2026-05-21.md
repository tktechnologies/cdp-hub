# N8N MCP Audit — 2026-05-21

> **Historical.** `cdp_analise` → `cdp_router`, `cdp_resultado` → `cdp_scraper`. Canonical: [docs/n8n/LIVE_WORKFLOWS.md](../../../docs/n8n/LIVE_WORKFLOWS.md).

Read-only audit via n8n MCP (2026-05-21 morning). **A28 published same day** — drift
resolved; see `docs/validation/A28_n8n_publish_20260521.md`.

## Live workflows

| Name | ID | Active | MCP |
|------|-----|--------|-----|
| `cdp_analise` | `6id6dkinK9xTLfsb` | yes | yes |
| `cdp_resultado` | `VfBSV3WU6on8BXm8` | yes | yes |
| `cdp_muvstok-api_receiver` | `t160mzGPYYlJcrjZ` | yes | yes |
| `cdp_muvstok-api_starter` | `PXLHDzRbBVgs8Xl2` | no | yes |

## Critical drift: `cdp_analise`

| Check | Live | Repo export |
|-------|------|-------------|
| Endpoint | `POST /api/v1/jobs` | Same |
| Body fields | `items`, `sites`, `callback_url`, **`job_metadata`**, `priority` | `items`, `sites`, `callback_url`, `priority` only |
| Default sites | `gm`, `vw`, `eu`, **`ebay`**, `pecadireta`, `melibox` (no `ml`) | `gm`, `ml`, `vw`, `eu`, `pecadireta` |
| Batch formatter | v0.8.5 (embeds `job_metadata`) | v1.0 (query params on `callback_url`) |

API does not persist arbitrary `job_metadata` in callbacks — repo contract is correct.

## Critical drift: `cdp_resultado`

| Check | Live | Repo export |
|-------|------|-------------|
| Webhook path | `scraper-result` | Same |
| Secret verification | `rightValue: "changeme"` | `$env.CDP_CALLBACK_WEBHOOK_SECRET \|\| ...` |

Production Key Vault secret matches trimmed manual tests; live IF node may still compare against `changeme` depending on active graph version.

## Callback E2E (same day)

- Worker automated callback: **failed** (`\r` in secret header).
- Manual webhook with trimmed secret: execution **354** / **361** `success`.
- Full 5-SKU payload received in webhook body when delivered manually.

## Recommended actions

1. Deploy scraper image with `src/config.py` credential strip.
2. After user says **publish**: MCP `update_workflow` from `n8n/workflows/cdp_*.json` following `n8n-release-preflight.md`.
3. Re-run `.agent/prompts/n8n-cache-integration-test.md` Phase 3 after deploy.
