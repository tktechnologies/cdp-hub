# n8n (moved)

Workflow JSON, router Code, and receiver helpers live at the **monorepo root**:

- `../../n8n/workflows/`
- `../../n8n/src/`
- `../../n8n/lib/`

Scraper-specific n8n docs remain under `docs/` in this directory.

Sync: `make sync-n8n` from repo root (requires user approval to publish).
