# Specs Commands

## Check Required Specs

```bash
make check-specs
```

Equivalent:

```bash
bash scripts/check_specs.sh
```

Expected output: `All required specs exist.`

Update `scripts/check_specs.sh` when a required spec is renamed, added, or removed.
