# Tests

Unit and contract tests run locally via `uv run pytest tests/ -v`. See [`.agent/commands/run-quality-gates.md`](../../.agent/commands/run-quality-gates.md).

Official production validation still requires Azure-hosted environments; do not treat local pytest alone as the final signal for production work.
